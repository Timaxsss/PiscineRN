/** @type {import("prettier").Config} */
const config = {
    bracketSpacing: false,
    singleQuote: true,
    trailingComma: 'none',
    tabWidth: 4,
    bracketSpacing: true,
    arrowParens: 'always'
};

module.exports = config;
